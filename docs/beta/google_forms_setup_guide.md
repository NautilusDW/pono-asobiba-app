# ポノのあそびば βテスト Google Forms 設定手順書

> 想定所要時間: **約10分 × 2本 = 20分**
> 並走するコピペシート: [`google_forms_template.md`](./google_forms_template.md)
> 困ったら末尾 **FAQ / トラブルシュート** へ。

---

## 0. 用語ミニ辞典 (最初に 30秒)

| 用語 | 意味 |
| --- | --- |
| **Google Forms** | Google のアンケート作成ツール。 ブラウザだけで使える、 無料。 |
| **質問形式 (回答形式)** | ラジオボタン / チェックボックス / 記述式 / 段落 / 均等目盛 など、 回答の入力方法。 |
| **ラジオボタン** | 1つだけ選べる丸印。 「単一選択」。 |
| **チェックボックス** | 複数選べる四角印。 「複数選択可」。 |
| **記述式 (短文回答)** | 1行のテキスト入力。 |
| **段落 (長文回答)** | 複数行のテキスト入力。 |
| **均等目盛** | 1-5 や 0-10 のような 段階評価のバー。 |
| **セクション** | 質問のグループ分け。 ページ区切りにもなる。 |
| **事前入力リンク** | URL に値を埋め込んでおいて、 回答者が開いた時に最初から入力された状態にする仕組み。 fid 自動付与に使う。 |
| **entry ID** | 各質問の内部 ID (例: `entry.123456789`)。 事前入力リンクに必要。 |
| **応答 (Responses)** | 集まった回答データ。 スプレッドシートに自動連携できる。 |

---

## 1. 中間アンケートを作る (10分)

### Step 1-1. 新規フォームを作成 (1分)

1. ブラウザで <https://forms.google.com> を開く
2. 左上の **「空白のフォーム」** (+ アイコン) をクリック
3. 画面上部の **「無題のフォーム」** をクリック → タイトルを以下に変更:
   ```
   ポノのあそびば おためし版 中間アンケート
   ```
4. タイトル直下の **「フォームの説明」** をクリック → コピペシートの「説明文」 をペースト

### Step 1-2. 匿名化設定 (1分)

1. 画面上部 **「設定 (Settings)」** タブをクリック
2. **「回答」** セクションを展開:
   - **「メールアドレスを収集する」** → **OFF** (回収しない)
   - **「ログインを必須にする」** → **OFF** (Google アカウントを要求しない)
   - **「回答を 1 回に制限する」** → **OFF** (※ON にすると Google ログイン必須になるため、 βでは OFF 推奨。 fid で重複検出する設計)
3. **「プレゼンテーション」** セクションを展開:
   - **「進行状況バーを表示」** → **ON**
   - **「確認メッセージ」** にコピペシート末尾の確認メッセージをペースト

### Step 1-3. 質問を順番に追加 (5-6分)

1. 上部 **「質問」** タブに戻る
2. デフォルトで 1つ目の質問枠が表示されているので、 **コピペシートの Q0 (fid)** から順に作成
3. 各質問の作成手順:
   1. **質問テキスト欄**にコピペシートの「質問タイトル」 を貼る
   2. 右上の **質問形式プルダウン**をクリックして、 コピペシートの「回答形式」 を選ぶ
   3. 選択肢タイプ (ラジオ/チェックボックス) なら 「選択肢を追加」 を繰り返してコピペシートの選択肢を 1行ずつ貼る
   4. 右下 **「必須」** トグルをコピペシートに従って ON/OFF
   5. 質問枠の右下 **「︙」 (3点メニュー) → 「説明 (Description)」** を ON にして、 ヘルプテキストを貼る (必要な場合のみ)
   6. 右側ツールバーの **「+」 (質問を追加)** をクリックして次の質問へ
4. Q5 (最終問) まで完成したら、 画面右上 **「目のアイコン (プレビュー)」** で全体確認

### Step 1-4. fid の entry ID を取得 (★ 重要・2分)

> fid を URL から自動付与するために必要な作業。 1回だけで OK。

1. 画面右上 **「︙」 (3点メニュー)** をクリック
2. **「フォームに事前入力する」** をクリック (英: `Get pre-filled link`)
3. プレビュー画面が開く → **Q0 の fid 欄**に仮の値 `SAMPLE` を入力 (他の必須欄も適当に埋める)
4. 下部の **「リンクを取得」** ボタンをクリック
5. 上部に出てくる URL を **「リンクをコピー」** でコピー
6. メモ帳などに貼り付けて、 `entry.数字=SAMPLE` の部分を探す
   - 例: `https://docs.google.com/forms/d/e/xxx/viewform?entry.1234567890=SAMPLE&entry.9876543210=...`
   - この `entry.1234567890` が **fid 用の entry ID**
7. **`entry.1234567890` を控える** (この後 fid 発行スクリプト/配布物 URL に使う)

### Step 1-5. 公開 URL を取得 (1分)

1. 画面右上 **「送信」** ボタンをクリック
2. 上部タブで **「リンク」 アイコン** (鎖マーク) を選択
3. **「URL を短縮」** にチェックを入れると `https://forms.gle/xxxxx` の短い URL が得られる (任意)
4. **「コピー」** で URL を取得 → 控える

### Step 1-6. スプレッドシート連携 (1分)

1. 画面上部 **「回答 (Responses)」** タブをクリック
2. **「スプレッドシートにリンク」** (緑のアイコン) をクリック
3. **「新しいスプレッドシートを作成」** を選択 → デフォルト名でOK → **「作成」**
4. 新しいタブでスプレッドシートが開けば成功 → 以後、 回答が自動で行追加される

---

## 2. 最終アンケートを作る (10分)

### 画像ファイルの準備 (★ 着手前に必ず実施・2分)

> Q2 のゲームアイコン画像を 5枚 事前にダウンロードしておく。 これを飛ばすと作業途中で詰まり、 10分以内完成の約束が守れません。

1. **入手元**: GitHub リポジトリ `assets/ui/title/` フォルダ
   - URL: <https://github.com/ndwss2023/pono-asobiba-app/tree/master/assets/ui/title>
   - またはローカルクローン済みであれば `D:\AppDevelopment\pono-asobiba-app\assets\ui\title\` から直接コピー
2. **ダウンロードすべきファイル (5枚すべて)**:
   ```
   bento.png
   puzzle.png
   maze.png
   quizland.png
   oto.png
   ```
   ※「シールちょう」「どれもだいたい同じくらい」「あまり遊ばなかった」 の 3選択肢は画像なしでテキストのみ
3. **形式の確認**: 拡張子が **必ず `.png`** であること
   - `.webp` ファイルがあっても **使わない** (Forms で「壊れた画像」 と表示されることがある: FAQ 参照)
   - PNG 版が見当たらない場合は ndwss2023@gmail.com に連絡 → 即変換して再配布
4. ダウンロード先は デスクトップなど分かる場所に **`pono-icons` フォルダ**を作って 5枚まとめて入れておく (アップロード時にすぐ選べる)

---

基本は中間と同じ手順 (Step 1-1 〜 1-6)。 違いだけ列挙:

- **タイトル**: `ポノのあそびば おためし版 最終アンケート`
- **質問数**: 14 (fid + Q1-Q13)
- **セクション (ページ区切り) を 4つ挿入**:
  - セクション 1 (Q1 の前): `1. お子さまの体験について (4問)`
  - セクション 2 (Q5 の前): `2. 保護者の方のご評価 (5問)`
  - セクション 3 (Q10 の前): `3. 今後についての ご意向 (3問)`
  - セクション 4 (Q13 の前): `4. 自由記述 (1問)`
  - **挿入手順**: 右側ツールバーの **「セクションを追加」** (二本線アイコン) → セクションタイトルと説明を貼る
- **Q2 (一番くりかえし遊んだゲーム) に画像添付**:
  1. Q2 の選択肢を 8個 すべて入力する
  2. 各選択肢の**右端にマウスを当てる**と現れる **「画像アイコン (山と太陽の絵)」** をクリック
  3. **「アップロード」** タブを選択
  4. **「画像ファイルの準備」** で `pono-icons` フォルダにまとめた PNG (`bento.png` 等) を 該当する選択肢ごとにアップロード
  5. アップロード後、 選択肢の横にサムネイルが表示されれば成功
  6. 5つの画像対応選択肢すべて繰り返す (「シール帳」「どれもだいたい同じくらい」「あまり遊ばなかった」 は画像なし)
  7. **※ 万一 画像準備を飛ばしてしまった場合のみ**: テキストのみで先に進めて構わないが、 配布前の最終チェックリストで「Q2 のゲームアイコン 5枚 表示確認」 が必ず必要。 原則は事前準備を完了してから着手すること。
- **均等目盛 (Q1, Q5, Q8, Q9, Q11) の設定手順**:
  1. 質問形式プルダウンで **「均等目盛」** を選択
  2. 数値範囲を指定 (Q11 のみ 0-10、 他は 1-5)
  3. 左端ラベル / 右端ラベル を コピペシートに従って入力

- **fid の entry ID は中間と別**: 同じ手順 (Step 1-4) でこのフォーム用の `entry.XXXXX` を取得 → 控える

---

## 3. 配布前の最終チェックリスト

両フォーム完成後、 以下を控えておくと配布スクリプト/チラシ作成がスムーズです:

### ✅ チェックリスト

- [ ] 中間アンケート **公開 URL**: `https://forms.gle/__________`
- [ ] 中間アンケート **Form ID** (URL の `/d/e/` と `/viewform` の間): `__________`
- [ ] 中間アンケート **fid 用 entry ID**: `entry.__________`
- [ ] 中間アンケート **回答スプレッドシート URL**: `__________`
- [ ] 最終アンケート **公開 URL**: `https://forms.gle/__________`
- [ ] 最終アンケート **Form ID**: `__________`
- [ ] 最終アンケート **fid 用 entry ID**: `entry.__________`
- [ ] 最終アンケート **回答スプレッドシート URL**: `__________`
- [ ] 両フォームともプレビューで全質問が表示されることを確認
- [ ] 両フォームともテスト回答を 1件 送信し、 スプレッドシートに行が追加されることを確認
- [ ] 事前入力リンク (`?entry.XXXX=TESTFID`) でアクセスすると fid 欄が `TESTFID` で埋まることを確認
- [ ] Q2 (最終アンケート) のゲームアイコン 5枚 が全て表示されることを確認

### 配布 URL の組み立て方

fid `ABC234` の家族に最終アンケートを送る場合:

```
https://docs.google.com/forms/d/e/[FORM_ID]/viewform?entry.[ENTRY_ID]=ABC234
```

これをそのまま LINE/メール/アプリ内バナーから開かせれば fid が自動入力されます。

---

## 4. FAQ / トラブルシュート

### Q. 「フォームに事前入力する」 が見つかりません
A. 画面右上の **「︙」 (縦3点メニュー)** の中にあります。 質問編集中ではなく **フォーム全体の編集画面** (URL が `/edit` で終わる画面) で開いてください。 セクション内の質問の「︙」 とは別物です。

### Q. fid 欄を回答者に編集されたくない
A. 残念ながら Google Forms には「読み取り専用フィールド」 機能がありません。 ヘルプテキスト (`このコードは自動で入力されます。 そのまま次に進んでください。 (空欄でも回答できます)`) で抑止し、 集計時に短縮 URL とのクロスチェックで弾く運用にします。 万一改ざんがあっても fid 長 6文字 base32 + 配布リスト突き合わせで検出可能です。

### Q. 画像をアップロードしたのに 「壊れた画像」 マークが出る
A. 画像形式が `webp` の場合、 まれに Forms が表示できないことがあります。 PNG または JPEG に変換してから再アップロードしてください (このプロジェクトは `assets/ui/title/` 下に PNG 版があれば PNG を優先)。

### Q. 「回答を 1回に制限」 を ON にできない
A. Google アカウントへのログインが必須になる設定のため、 βテストでは **OFF** が正解です。 重複回答の検出は fid (匿名 ID) で行います。

### Q. スプレッドシート連携を忘れたまま回答が来てしまった
A. 後から連携しても **過去の回答も自動で取り込まれる**ので大丈夫です。 「回答」 タブ → 「スプレッドシートにリンク」 を後から実行してください。

### Q. 質問の順番を入れ替えたい
A. 質問枠を **マウスでドラッグ** (枠の上端の点々が並んだ部分をつかむ) すれば移動できます。 ただし fid 質問は **必ず先頭** に置いてください (entry ID は変わりませんが、 回答者が混乱しないため)。

---

## 5. 完成後の運用フロー (参考)

1. **Day 0 (配布開始)**: チラシ/同意書/アプリ起動時に `?fid=XXXXXX` 付き URL を配布
2. **Day 7 (中間アンケート配信)**: アプリ内バナー + メール で中間アンケート URL (fid prefill 付き) を案内
3. **Day 14 (最終アンケート配信)**: 同様に最終アンケート URL を案内
4. **Day 14-16**: スプレッドシートで集計、 go/no-go 判定:
   - D7 継続率 ≥ 40%
   - 「😀すごく良い + 🙂良い」 (Q10) ≥ 60%
   - NPS 7以上 (Q11) ≥ 30%
5. **Day 17-21**: 創設メンバー特典を配布 (シール即配布 / 永久無料権付与)

---

以上で βテスト用 Google Forms のセットアップは完了です。 ご不明点は ndwss2023@gmail.com まで。 一人で開発しているため、 お返事にお時間をいただく場合があります。
