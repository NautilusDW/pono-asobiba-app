# 畑 → クッキング → お弁当 一気通貫プラン

作成日: 2026-07-23
対象: `hatake-nikki/` / `bento/kitchen.html` / `bento/index.html`
現状: 畑の3×3・9区画構成、看板、水やり済み表示は batch:1434 までに実装済み。batch:1439 で、お弁当レシピから逆算した初期4作物と、作物固有の種から収穫までの画像を実装する。共通在庫とクッキング消費は後続Phase。

## 1. 結論

「育てる → 収穫する → 料理する → お弁当に入れる」を一本の体験にする。畑の初期作物は、実装済みのお弁当へ直接つながる `tomato`／`ninjin`／`potato`／`onion` の4種に絞る。この4種で9レシピを覆い、5種目はブロッコリーを候補とする。

画像と畑操作は4種を一度に揃えるが、共通在庫を使う最初の一気通貫実装は「とまと → プチトマト → お弁当」の縦切り1本から始める。既存のクッキングとお弁当は「何度でも自由に作れる」遊びなので、畑在庫を参加条件にしない。畑の野菜がある時だけ「自分で育てた食材を使った」印と一気通貫ごほうびを追加する。

## 2. 体験フロー

1. 畑で種を選び、絵付き看板で区画を見分ける。
2. 水やりとお世話で育てる。
3. 収穫すると「はたけの やさい」が1こ増える。
4. 収穫後の非ブロッキング導線からクッキングへ移る。
5. 該当レシピに「はたけの やさい 1こ」の印を出す。
6. 料理を最後まで完了した時だけ、生野菜1こを「つくった おかず」1こへ変換する。
7. お弁当で同じおかずを置くと「つくった」印を出す。
8. お弁当の「できあがり」時に、実際に入っている畑由来のおかずを、種類ごとに最大1食分使い、一気通貫の完了演出を出す。

## 3. 作物・レシピ・お弁当の対応

| 導入順 | 畑の作物ID | 共通在庫ID | 対応レシピID | お弁当名 | 理由 |
| --- | --- | --- | --- | --- | --- |
| 1 | `tomato` | `tomato` | recipe `tomato` | `プチトマト` | 既存の洗う・へたを取る・カップに入れる工程と直結し、最短で通せる |
| 2 | `ninjin` | `carrot` | `ninjin_ingen` / `kinpira` / `potato_salad` / `harumaki` | `にんじんいんげん` / `きんぴらごぼう` / `ポテトサラダ` / `はるまき` | 4レシピで使われ、切る・炒める・混ぜる体験へ広げやすい |
| 3 | `potato` | `potato` | `korokke` / `potato_salad` | `コロッケ` / `ポテトサラダ` | 地中で育つ違いが見え、収穫後の調理工程も豊か |
| 4 | `onion` | `onion` | `hamburg` / `meatball` / `napolitan` | `ハンバーグ` / `ミートボール` / `ナポリタン` | 主菜と麺の両方へつながり、料理との関係が分かりやすい |

`hatake-nikki` の既存ID `ninjin` はセーブ互換のため変更しない。作物マスターへ `inventoryId: 'carrot'` を追加して吸収する。

ワークショップ系レシピは現状、表示文字列だけで材料を持つものや `needs: []` のものがある。畑連携で表示名から推測せず、レシピごとに安定IDの `farmNeeds` を明示する。ポテトサラダのような複数作物レシピは、原則として `farmNeeds: { potato: 1, carrot: 1 }` のように、畑由来印を付ける時だけ対応作物をすべて消費する。

### 3.1 種から収穫までの画像段階

4種とも次の5段階を別画像で持つ。

| 段階 | 状態 | 子どもが見分ける手掛かり |
| --- | --- | --- |
| 0 | たね | とまとの淡い種、にんじんの小粒、種いも、たまねぎの黒い種 |
| 1 | め | 双葉、じゃがいもの芽、たまねぎのループ状の芽 |
| 2 | 小さな株 | 作物固有の葉 |
| 3 | できはじめ | とまとの花と青い実、にんじんの根の肩、じゃがいもの花、たまねぎの小さな球 |
| 4 | しゅうかくOK | 完熟した実・根・いも・球 |

従来はロジック上4段階でも、種と芽が絵文字、育ち中と収穫OKが同じ完成野菜画像だったため、見た目では途中が1段階に感じられた。batch:1439では5枚を本当に描き分ける。一方、幼児が待ち疲れないよう、実時間の待ち日数を画像枚数と同じだけ増やすことはしない。日々のお世話のリズムは維持し、初回の変化が見えない待ち時間を作らない進行を採用する。

## 4. UI方針

### 4.1 畑

- 畑は論理3×3の合計9区画。アイソメ上で奥から「1・2・3・2・1」に見える左右対称なひし形へ投影し、畑面の中央へ置く。各区画の間には細い緑のすき間をあぜ道として残す。
- 9区画は最初からすべて使用可とし、区画の段階解放は入れない。成長要素は土地の枚数ではなく、今後追加する種・料理・お弁当の対応で作る。
- 旧3／4区画セーブはindex 0〜2／0〜3と従来の「奥・左・右・手前」の位置を保持し、index 4〜8を空畑で補完する。保存キー `pono_hatake_state_v1` は変更しない。
- 看板は各 `.plot` の左角寄りに置き、種を植えた直後から収穫直前まで同じ絵を表示する。
- 空き区画と収穫直後は看板を隠す。
- 看板は `pointer-events: none`、装飾画像は `alt=""` / `aria-hidden="true"`。区画のひし形操作面は実button、または `role="button"` / `tabindex="0"` と Enter / Space / mouse click操作を備え、`aria-label` は「いちばん おく／おくの ひだり／…／いちばん てまえ＋作物名」で9区画を読み分けられるようにする。
- 水やり済み印は看板と共通の `.plot-marker` 内に置き、看板の左上外側へ小さく重ねて「この札の畑は水やり済み」と一体で読ませる。とまと／にんじんの絵とは重ねず、湿った暗色土と水色外周も併用して印だけに依存しない。
- 虫は畝の右上、作物は中央を維持し、看板・水印のまとまりと役割を分ける。
- 収穫の宝箱を閉じた後、畑HUDに短時間だけ `クッキングへ` と `まだ おせわする` を出す。

### 4.2 クッキング

- 畑在庫がある対応レシピにだけ、小さな `はたけの やさい 1こ` 印を出す。
- 中途離脱、リロード、「もどる」では在庫を減らさない。
- レシピの完了コミット時だけ、生野菜を1こ減らし、調理済みの「1食分」を1こ増やす。
- 別タブで先に使われて在庫0になっても、料理の完了は止めない。その場合は通常の自由調理として完了する。

### 4.3 お弁当

- 調理済み在庫があるおかずカードに `つくった 1こ` 印を出す。
- 配置時は在庫を減らさない。置いてから取り消した時に「食材が戻る」特別処理を不要にする。
- `stageComplete()` 時に完成弁当内の名前を見て、対応する異なるおかずIDごとに最大1食分使う。同じおかずを複数置いても「1食分」の在庫は1つだけ使う。
- 在庫競合で消費できなくても、お弁当の完成は止めない。
- 完了処理は弁当ラウンド開始時に作った安定 `completionOpId` を使う。結果を `completionReceipt`（`consumedPreparedIds` を含む）として保持し、完成画面は減算後のlive在庫ではなくこのreceiptから「つくった」印と一気通貫演出を出す。

## 5. 推奨データ設計

作物の収穫やレシピ完了を「その場限りの解除」ではなく、共通在庫として保存する。新規キーは `pono_food_inventory_v1`。

```js
{
  version: 1,
  revision: 0,
  updatedAt: 0,
  raw: {
    tomato: { available: 0, earned: 0, used: 0 },
    carrot: { available: 0, earned: 0, used: 0 },
    potato: { available: 0, earned: 0, used: 0 },
    onion: { available: 0, earned: 0, used: 0 }
  },
  prepared: {
    petit_tomato: { available: 0, made: 0, packed: 0 },
    ninjin_ingen: { available: 0, made: 0, packed: 0 },
    kinpira: { available: 0, made: 0, packed: 0 },
    korokke: { available: 0, made: 0, packed: 0 },
    potato_salad: { available: 0, made: 0, packed: 0 },
    hamburg: { available: 0, made: 0, packed: 0 },
    meatball: { available: 0, made: 0, packed: 0 },
    napolitan: { available: 0, made: 0, packed: 0 },
    harumaki: { available: 0, made: 0, packed: 0 }
  },
  appliedOps: {}
}
```

`appliedOps` は同じ操作IDの二重加算・二重消費を防ぐ。無限増大を避けるため、確定済みの新しい操作だけ128件程度にローテーションする。

### 5.1 複数タブでも失わない更新規約

`revision` と `appliedOps` だけでは、2タブが同時にread-modify-writeした時のlast-write-winsを防げない。すべての在庫mutationを次の共通経路へ集約する。

1. 収穫、料理run、お弁当roundの開始時に安定した `opId` を作り、各ゲームのセーブへ先に保存する。完了関数が二重に呼ばれても同じ `opId` を使う。
2. `navigator.locks.request('pono-food-inventory-v1', { mode: 'exclusive' })` の中で、在庫を再読込 → `appliedOps` 確認 → mutation → 保存する。
3. lock取得前に読んだ在庫値は判定に使わない。`revision` は監査と再試行検知に使い、排他制御の代わりにはしない。
4. Phase 1開始時に対象WebView / Safari / ChromiumのWeb Locks対応を実機確認する。未対応対象が1つでもある場合は、単純なlocalStorage leaseで代用せず、IndexedDB transactionを在庫の正本にして、非同期の書き出し／復元まで同Phaseで整備する。
5. `appliedOps` を削除できるのは、対応するproducer outboxがack済みで、復元データにも未処理opがないことを確認できる場合だけにする。

### 5.2 収穫と図鑑の消失防止

`hatake-nikki` の畝セーブと共通在庫は別localStorageキーなので、単純な2回書き込みだと中断時に収穫品を失う。畑セーブ側に `inventoryOutbox` を持つ。

1. 現在stateを直接破壊せず、「畝を空にする変更＋固有 `opId` 付き収穫操作」を含む次stateを作る。
2. `saveState(nextState)` は成功可否を返し、書き込みと読戻し確認に成功した時だけ画面stateを切り替える。失敗時は畝を空にせず、収穫演出も出さない。
3. outboxの同じ `opId` で、共通在庫への加算と図鑑count / firstDateをそれぞれ冪等適用する。図鑑側にも適用済みopを持たせるか、在庫の `earned` から導出する。
4. 在庫と図鑑の両方が保存成功した後だけoutboxをackして削除する。
5. 畑起動時に残ったoutboxを再実行する。

この方式なら、どの行間でアプリが閉じても、次回起動で回復し、二重には増えない。

## 6. 段階導入

### Phase 0: 畑の視認性と作物画像（batch:1425 / 1427 / 1430 / 1431 / 1434 / 1439）

- 最初から使える9区画を `green.png` の1200×670面へ論理3×3（表示1／2／3／2／1）で中央配置し、区画間にあぜ道を残す。
- とまと／にんじん／じゃがいも／たまねぎのアイソメ立て看板をGPT Image 2で作成。
- 透過分割した看板と生成しずくを、作物・水やり状態へ同期して表示する。
- 4作物それぞれに、種／芽／小さな株／できはじめ／収穫OKと種選択画像を用意し、作物マスターから描画する。
- 種ツールを絵文字から4区画の木製種箱画像へ置き換える。

### Phase 1: 収穫在庫の土台

- `common/food-inventory.js` と純粋ロジック回帰テスト。
- `hatake-nikki` の `inventoryOutbox` と旧セーブ正規化。
- shared lock、安定 `opId`、図鑑を含む耐久収穫、再起動、二重適用、保存失敗をテスト。
- `common/` はClaude主担当領域のため、このPhase着手前に調整する。

### Phase 2: とまと縦切り

- 収穫後の `クッキングへ` 導線。
- recipe `tomato` へ畑由来印と完了時変換を追加。
- お弁当の `プチトマト` と完成時消費を接続。
- 一気通貫完了ごほうびとローカル実績を追加。
- `bento/` はnativeへ既に収録されているため、畑本体のnative収録を待たず、このPhaseで新規 `common/food-inventory.js` を `native/content-manifest.json` と参照検証へ追加する。

### Phase 3: 4作物へ展開

- `ninjin -> carrot -> ninjin_ingen` の明示マッピング。
- にんじんいんげんの完了時変換とお弁当完成時消費。
- `potato`／`onion` と対応6レシピへ同じ安定ID契約を展開。
- 複数作物を使うポテトサラダ／はるまきは、明示した `farmNeeds` の全作物を同じ料理opで冪等消費する。
- 在庫が複数ある時、複数レシピが同じ作物を使う時の競合をテスト。

### Phase 4: 発展

- 5種目はブロッコリーを優先し、その後にキャベツ、えだまめ、いんげん等を検討する。
- 「そだてた やさいで おべんとうを つくろう」の週替わりミッション。
- 子どもが作物とおかずの関係を見返せる「はたけから おべんとうまで」図鑑。

## 7. 子ども向け表示文言

漢字は使わず、短いかな／カタカナにする。

| 場所 | 文言 |
| --- | --- |
| 収穫 | `しゅうかくしたよ！` |
| 収穫後 | `クッキングへ` / `まだ おせわする` |
| レシピ印 | `はたけの やさい 1こ` |
| 料理完了 | `つくった おかずが できた！` |
| お弁当印 | `つくった 1こ` |
| 一気通貫完了 | `はたけから おべんとうまで できた！` |

## 8. tier方針

- App staging / app tier: 畑→クッキング→お弁当の連携を表示。
- LP staging の free / book tier: 畑由来印、導線、専用ごほうびを表示しない。既存のお弁当体験は変更しない。
- インポートデータに在庫があっても、それを理由に食材やレシピのtierロックを解除しない。
- MVPで `common/tier.js` のアンロック集合を変更しない。呼び出し側の `PonoTier.isApp()` で表示を制御する。

## 9. セーブ・同期・復元

- `pono_food_inventory_v1` をデータ書き出し／復元の対象にする。
- 旧端末からの復元でキーがない時は0在庫を作る。
- 不正な数値、未知ID、過大値は正規化する。在庫は0未満にしない。
- `common/data-export.js` の `CORE_PRESERVE_IF_ABSENT` とクラウド同期スナップショットの対象を見直す。
- Phase 2でbentoから新規common在庫モジュールを読む時点で、`native/content-manifest.json` と参照検証を更新する。畑本体をnativeへ入れる時は、別途 `hatake-nikki/` 一式も追加する。

## 10. テスト計画

### 畑／看板

- 667×375 / 844×390 / 1024×768 / 1366×768で9畝すべての4頂点が `green.png` のひし形面内。
- `#plot-area` のx座標／幅／中心／比率が、`background-size: contain` の `green.png` 表示面と一致する。
- 9畝が奥から1／2／3／2／1の等間隔で畑面中央に並び、各中心が自身の操作面になり、あぜ道と透明角が別の畝への誤操作を起こさない。
- 旧3／4区画セーブを読み込むと既存区画と従来位置を保持したまま9枠へ空畑が追加され、末尾区画も植栽・水やり・収穫できる。
- キーボードだけで初回チュートリアル、種選択、植栽、水やりまで完走でき、マウスclickとtouchの合成clickが二重発火しない。
- 各区画を奥から手前へ一意な9位置名つきで読み上げ、同じ作物を複数植えても区別できる。
- 390×844のタッチ縦画面は横向き案内が全面を覆う。
- 4作物で正しい看板が出る。空き区画と収穫後は消える。
- 植えた直後から作物固有の種画像が出て、5段階のURLと画像が重複しない。
- 5段階すべてで根元が畝中央に固定され、収穫OKだけが発光する。
- 看板とその左上の水やり済み印が一体で追従し、どちらも水やり長押し、虫フリック、収穫タップを遮らない。
- 看板＋水やり済み印のまとまりが、中央の作物・右上の虫・隣接畝を覆わない。

### 在庫

- 収穫で生野菜が1回だけ増える。
- outbox残存後の再起動で回復し、同じ `opId` は二重加算しない。
- 壊れた在庫JSONでクラッシュしない。
- 書き込み失敗時に収穫品を消失しない。
- 2 browser contextから同時収穫／同時調理しても加算・消費・`appliedOps`が失われない。

### クッキング／お弁当

- 料理途中の離脱・再読込みで生野菜が減らない。
- 料理完了で「生野菜 -1 / 調理済み +1」が1回だけ適用される。
- 同じ料理完了／`stageComplete()` を2回呼んでも、安定 `opId` により1回しか消費しない。
- お弁当で置いて取り消しても在庫は減らない。
- 完成時に実際に入った対応おかずがある時だけ、調理済みを1食分使う。
- 複数種類を入れた時は種類ごとに最大1食分を使い、receiptの `consumedPreparedIds` と完成演出が一致する。
- 在庫0でも従来の料理／お弁当遊びが完走する。
- free / bookでapp専用印やロック食材が漏れない。
- native同期後、`common/food-inventory.js` が404にならずbentoを完走できる。

## 11. 分析とプライバシー

MVPで新しい外部テレメトリーを追加しない。まずは既存のローカル実績カウントで「収穫」と「一気通貫完了」を確認できるようにする。ファネルを外部送信する場合は、`src/api/events.js` のallowlist、`docs/data-analytics-plan.md`、Apple Kidsカテゴリの申告・プライバシー要件を別タスクで見直す。

## 12. 実装前の確定ゲート

以下はスキーマ変更を伴うため、Phase 1着手前にユーザーと最終確定する。

1. 畑由来食材を「消費する特別ルート」とし、従来の無限の自由遊びを残す。
2. 調理済み1こを「おかず1個」ではなく「お弁当に使える1食分」として扱う。
3. 畑の画像と種選択は4作物を実装し、共通在庫を使う最初の縦切りはとまとに限定する。
4. `pono_food_inventory_v1` と `inventoryOutbox` を新設する。
5. 完成弁当では、同じ種類を複数置いても1食分、異なる畑由来おかずは種類ごとに1食分を使う。
6. 全mutationをshared lock内で行い、Web Locks非対応対象があればIndexedDB transactionへ切り替える。
7. 収穫outboxは在庫だけでなく図鑑更新まで同じ `opId` で完了してからackする。
